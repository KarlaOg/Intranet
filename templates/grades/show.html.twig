{% extends 'base.html.twig' %}

{% block title %}Grades{% endblock %}

{% block body %}
    <h1>Grades</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td >{{ grade.id }}</td>
            </tr>
            <tr>
                <th>Grade</th>
                <td>{{ grade.grade }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ grade.comment }}</td>
            </tr>
            <tr>
                <th>Course</th>
                <td>{{ grade.course }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('grades_index') }}">back to list</a>

    <a href="{{ path('grades_edit', {'id': grade.id}) }}">edit</a>

    {{ include('grades/_delete_form.html.twig') }}
{% endblock %}
